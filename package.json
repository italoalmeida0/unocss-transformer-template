{
  "sideEffects": false,
  "type": "module",
  "version": "0.0.1",
  "name": "unocss-transformer-template",
  "author": "Italo Almeida",
  "description": "UnoCSS transformer for creating reusable CSS templates, variants, and components using @template-content, @template-variant, @template-component directives and @for loops.",
  "homepage": "https://github.com/italoalmeida0/unocss-transformer-template",
  "repository": {
    "type": "git",
    "url": "https://github.com/italoalmeida0/unocss-transformer-template.git"
  },
  "license": "MIT",
  "keywords": [
    "unocss",
    "unocss-transformer",
    "unocss-preset",
    "unocss-community",
    "css"
  ],
  "bugs": {
    "url": "https://github.com/italoalmeida0/unocss-transformer-template/issues"
  },
  "peerDependencies": {
    "@unocss/core": "^66.5.6",
    "unocss": "^66.5.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "dts-bundle-generator": "^9.5.1"
  },
  "scripts": {
    "build": "bun build ./src/index.mts --entry-naming=[dir]/index.mjs --outdir ./out/dist --packages external --production && npm run build:dts && node scripts/prepare-package.js",
    "build:dts": "dts-bundle-generator -o ./out/dist/index.d.mts ./src/index.mts --no-check  --external-imports=unocss,@unocss/core",
    "release": "npm run build && cd out && npm publish"
  },
  "main": "./dist/index.mjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "default": "./dist/index.mjs"
    }
  }
}
